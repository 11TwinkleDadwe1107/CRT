#include <iostream>
#include<algorithm>
using namespace std;
int main()
{
   int arr[]={3, 5, 1, 2, 4};
   sort(begin(arr), end(arr));// sort the array in ascending order
   //std::sort(std::begin(arr), std::end(arr));
   for(int i : arr)   //i=5
   {
    cout<<i<<" ";
   }
   return 0;
}
